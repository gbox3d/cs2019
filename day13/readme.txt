519p. 파일 입출력

파일 => 외부디바이스의 추상적 개념(소프트웨어적)
=> 쓰기 => pc->디바이스(하드디스크,플로피디스크) 로 전송
=> 읽기 => 디바이스-> pc 전송
=> I/O => event 데이터를 받았을때 통지=> 접속,종료 시점 구분

파일 열기 
=> FILE * => 파일 포인터 객체
=> FILE *fopen (파일이름,오픈형식(r,w,a) )
------------------------------------------
텍스트 파일 만들기
=>프로잭트에 대고 우클릭=> 추가=> 새항목=> 유틸리티
=> 텍스트 파일

바이너리 파일 저장과 복원(읽기)=>

저장 =>fwrite (버퍼,버퍼크기,버퍼갯수,파일포인터)

읽기 =>fread

-----------------------------
git pull, push
push => pc->cloud
pull => cloud -> pc
-----------------------------

mapTool 3일차
- save,load
- 커서위치에서 속성값 보기=>지정된 위치의 속성
- 직선 그리기
- 속성을 0x00으로 초기화

-------------------------------

2y = 3x

y = 1x

------------------------------------

FSM => 유한 기계 상태














