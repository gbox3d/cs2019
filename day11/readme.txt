행아웃 친구 추가 => 구글 행아웃

gbox3dgel3@gmail.com

=>새 대화=> 이메일 입력

--------------------------------------------------
421p 포인터 응용

다중 포인터

int **ppPtr;


429p 예제 15-1 

----------------------------------------
git 에서 특정 폴더 제외 시키기 
=> git 저장소의 최상위폴더에서 '.gitignore' 파일 만들기 
=> 제외 시키고 싶은 폴더 기록
=> 폴더 이름 뒤에 '/' 붙이기 =>예)
Debug/
.vs/
-----------------------------------------

441p 함수 포인터 

443p
int (*fp)(int ,int); => fp함수 포인터 
=> int 형 값을 반환하는 함수,인자로 int 형 2개를 받는함수
대한 포인터 변수

=> 받을수있는 함수의예
int max(int a,int b) -> o
int sub(int a,int b)  -> o
double mul(int a,int b) -> x
int div() -> x

447p. void 포인터

기존의 포인터 => 형을 특정
void * => 형을 특정하지않음 => 머든지 될수있죠
=> 줄기세포 포인터 

void 형식의 변수에 값을 읽고 쓰기=> 타입 캐스팅=> 

*(int *)ptr
*(double *)ptr
*(char *)ptr
((void (*)())ptr)()


함수 포인터 응용 => 문자열로된 명령어 핸들러 태이블 응용
=> 
---------------------------------------------
동적 할당 함수(malloc,free). exam5 457p.

void *malloc(unsigned int memory_size) 
=> memory_size는 1 바이트단위 크기 크기 지정
=> heap

=> maclloc으로 할당된 메모리는 반드시 반납해야된다.
=> free(void *ptr)

가변 배열 예제 496p.

























